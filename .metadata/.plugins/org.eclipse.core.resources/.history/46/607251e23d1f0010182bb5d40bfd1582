<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Reveal on Hover</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      background-color: #000;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: Arial, sans-serif;
    }

    .card-container {
      display: flex;
      gap: 40px;
    }

    .card {
      background-color: #fff;
      color: black;
      padding: 30px 50px;
      border-radius: 12px;
      box-shadow: 0 4px 12px rgba(255, 255, 255, 0.1);
      text-align: center;
      font-size: 24px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      cursor: pointer;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      height: 50px;
      width: 220px;
      overflow: hidden;
      position: relative;
    }

    .card:hover {
    	height: 150px;
      transform: translateY(-5px);
      box-shadow: 0 6px 18px rgba(255, 255, 255, 0.2);
    }

    .card-title {
      font-weight: bold;
      margin-bottom: 20px;
      transition: margin-bottom 0.3s ease;
    }

    .card-buttons {
      opacity: 0;
      transform: translateY(20px);
      transition: opacity 0.3s ease, transform 0.3s ease;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .card:hover .card-buttons {
      opacity: 1;
      transform: translateY(0);
    }

    .card button {
      width: 140px;
      padding: 10px 0;
      font-size: 14px;
      border: none;
      border-radius: 6px;
      background-color: #000;
      color: white;
      transition: background-color 0.3s ease;
      box-sizing: border-box;
    }

    .card button:hover {
      background-color: #333;
    }
  </style>
</head>
<body>
  <div class="card-container">
    <div class="card">
      <div class="card-title">JOB SEEKER</div>
      <div class="card-buttons">
        <button onclick="location.href='jobSeekerRegister.jsp'">Registration</button>
        <button onclick="location.href='jobSeekerLogin.jsp'">Login</button>
      </div>
    </div>

    <div class="card">
      <div class="card-title">COMPANY</div>
      <div class="card-buttons">
        <button onclick="location.href='companyRegister.jsp'">Registration</button>
        <button onclick="location.href='companyLogin.jsp'">Login</button>
      </div>
    </div>
  </div>
</body>
</html>
