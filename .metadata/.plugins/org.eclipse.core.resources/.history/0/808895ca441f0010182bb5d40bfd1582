<%@ page contentType="text/html; charset=UTF-8" language="java" %>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Page</title>

    <!-- Google Fonts - Poppins -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">

    <!-- Internal CSS -->
    <style>
        body {
            font-family: 'Poppins', sans-serif; /* Apply Poppins font */
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .registration-container {
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px;
            position: relative;
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
            font-weight: 500; /* You can customize the font weight */
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin-bottom: 8px;
            font-weight: 600;
        }

        input {
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }

        input:focus {
            border-color: #4d90fe;
            outline: none;
        }

        .submit-btn {
            background-color: #4CAF50;
            color: white;
            padding: 12px;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
        }

        .submit-btn:hover {
            background-color: #45a049;
        }

        .error {
            color: red;
            font-size: 14px;
            margin-bottom: 10px;
        }

        .file-upload {
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .file-upload input[type="file"] {
            padding: 5px;
        }

        /* Profile Picture - Circle View */
        .profile-pic-container {
            position: absolute;
            top: 10px;
            right: 20px;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            border: 3px solid #ccc;
            overflow: hidden;
            background-color: #e0e0e0;
            cursor: pointer;
        }

        .profile-pic-container img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* Hidden file input */
        #profilePicUpload {
            display: none;
        }
    </style>
</head>
<body>

    <div class="registration-container">
        <h2>Registration Form</h2>

        <!-- Profile Picture - Click to upload -->
        <div class="profile-pic-container" onclick="triggerFileInput()">
            <img id="profilePicPreview" src="https://via.placeholder.com/80" alt="Profile Picture">
        </div>
        <input type="file" id="profilePicUpload" name="profilePicUpload" accept=".jpg, .jpeg, .png" onchange="previewProfilePic()">

        <form id="registrationForm" action="register.jsp" method="post" enctype="multipart/form-data">
            <label for="fullName">Full Name</label>
            <input type="text" id="fullName" name="fullName" required>

            <label for="email">Email Address</label>
            <input type="email" id="email" name="email" required>

            <label for="password">Password</label>
            <input type="password" id="password" name="password" required>

            <label for="confirmPassword">Confirm Password</label>
            <input type="password" id="confirmPassword" name="confirmPassword" required>

            <label for="phone">Phone Number</label>
            <input type="tel" id="phone" name="phone" required>

            <!-- File Upload for other documents -->
            <label for="fileUpload">Upload File (Document)</label>
            <input type="file" id="fileUpload" name="fileUpload" class="file-upload" accept=".jpg, .jpeg, .png, .pdf, .docx" required>

            <button type="submit" class="submit-btn">Register</button>

            <div id="errorMessages" class="error">
                <%-- Here you can display error messages using JSP --%>
                <%= request.getAttribute("errorMessages") != null ? request.getAttribute("errorMessages") : "" %>
            </div>
        </form>
    </div>

    <!-- Internal JavaScript -->
    <script>
        // Function to trigger the file input click when the profile circle is clicked
        function triggerFileInput() {
            document.getElementById('profilePicUpload').click();
        }

        // Function to preview the uploaded profile picture
        function previewProfilePic() {
            const fileInput = document.getElementById("profilePicUpload");
            const file = fileInput.files[0];
            const preview = document.getElementById("profilePicPreview");

            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    preview.src = e.target.result;
                };
                reader.readAsDataURL(file);
            }
        }
    </script>

</body>
</html>
